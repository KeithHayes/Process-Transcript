
The python files will have to be corrected to use the llm through the API

transcript.txt is an audio transcript input to be formatted into complete sentences and paragraphs.

formatted.txt is the output which should be the input formatted into complete sentences and paragraphs.

Examine the python files and fix the code to format the input.

Current status is the llm is not formatting the input into output which has complete sentences and paragraphs.



