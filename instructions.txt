transcript_preprocessed.txt is sequentually sent to an llm model in two loops.

The two loop architecture must be preserved as a stub on it is unfinished.  For now changes to 
process.py must be minor and limited to those needed to tweak the output transcript_processed.txt to correctly 
resolve the issue of some words being run together which in the output of unformatted lines

Compare input to output and identify the needed tweeks.